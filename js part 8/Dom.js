/**
 * Dom
 * 
 * The DOM represents a documents while a logical tree
 * it allows us to manipulatelchanges webpage content (html elements).
 */

/**
 * Download starter code
 *
 * getElementById
 * Returns the element as an object or null (if not found)
 * 
 * document,getElementBYId(id)
 * 
 * 
 * getElementByClassName
 * Return the elementa as an html collection or empty collection (if not found)
 * 
 * document.getElementByClassName()
 * 
 * getElementByTagName();
 * return the element as an HTML collection or empty collection
 */

/**
 * Query selector
 * Allows us to use any css selector
 * 
 * document.querySelector('p') //selects first p element
 * document.querySelector('#myId')  //selects first element with id = myId
 * document.querySelector(',myClass'); //selector first element with class = myClass
 * 
 * document.querySelectorAll("p"); //selects all p elements
 */


/**
 * using properties & methods 
 * 
 * innerText 
 * shows the visibility text contained in a node 
 * 
 * textContent 
 * shows all the full text 
 * 
 * innerHTML 
 * Shows the full markup 
 * 
 * 
 * Manipulating Attributes
 * obj.setAttribute(attr,val);
 * obj.getAttribute(attr);
 */

/**
 * manupulating styling
 * 
 * style property 
 * obj.style.attribute = '';
 *  
 */