/*
array methods 

concat: merge 2 arrays
array.concat(2nd_array);

reverse: reverse an array 
array.reverse(); 
*/

let primary = ["red", "green", "yellow"];
let sec = ["black", "white", "blue"];

primary.concat(sec);

sec.reverse();