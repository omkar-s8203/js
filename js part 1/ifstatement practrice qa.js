/* 
Practice Qs

Qs) create a traffic light system that shows what to do based on color

*/

let color = "red";

if(color == "red"){
    console.log(`stop color is ${color}`);
}

if(color == "orange"){
    console.log(`ready color is ${color}`);
}

if(color == "green"){
    console.log(`go color is ${color}`);
}